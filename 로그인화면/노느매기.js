document.getElementById("myButton").addEventListener("click", function() {
    // Check the condition
    if (/* your condition here */) {
      // Apply the animation to the div element
      document.getElementById("myDiv").style.animation = "myAnimation 1s";
    }
  });